name: Test psaní v aplikaci

# Ruční test chatu proti živému Supabase (GitHub runner má síť, sandbox ne).
# Spustí se přes „Run workflow". Používá jen veřejný anon klíč, zkušební data
# na konci uklidí (smaže zkušební inzerát).

on:
  workflow_dispatch: {}

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Test chatu
        run: node scripts/test-chat.mjs
      - name: Test chytrých upozornění (Hlídání)
        run: node scripts/test-search.mjs
      - name: Test přidávání inzerátů (moderace)
        run: node scripts/test-listing.mjs
